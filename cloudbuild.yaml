serviceAccount: 'projects/gcpessesentials/serviceAccounts/cloud-build-sa@gcpessesentials.iam.gserviceaccount.com'

steps:
  # Build the Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - build
      - -t
      - europe-west1-docker.pkg.dev/gcpessesentials/inventory-repo/inventory-api:$COMMIT_SHA
      - -t
      - europe-west1-docker.pkg.dev/gcpessesentials/inventory-repo/inventory-api:latest
      - .

  # Push both tags to Artifact Registry
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - push
      - --all-tags
      - europe-west1-docker.pkg.dev/gcpessesentials/inventory-repo/inventory-api

  # Deploy to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - run
      - deploy
      - inventory-api
      - --image=europe-west1-docker.pkg.dev/gcpessesentials/inventory-repo/inventory-api:$COMMIT_SHA
      - --region=europe-west1
      - --platform=managed
      - --allow-unauthenticated
      - --port=8080
      - --memory=512Mi
      - --cpu=1
      - --min-instances=0
      - --max-instances=3
      - --set-env-vars=SPRING_PROFILES_ACTIVE=default

images:
  - europe-west1-docker.pkg.dev/gcpessesentials/inventory-repo/inventory-api:$COMMIT_SHA
  - europe-west1-docker.pkg.dev/gcpessesentials/inventory-repo/inventory-api:latest

options:
  logging: GCS_ONLY
  default_logs_bucket_behavior: REGIONAL_USER_OWNED_BUCKET
